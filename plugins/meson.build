plugindir = pkglibdir / 'plugins'

subdir('fileroller')
subdir('test')

if exiv2.found()
  subdir('exiv2')
endif
if gsf.found()
  subdir('gsf')
endif
if poppler.found()
  subdir('poppler')
endif
if taglib.found()
  subdir('taglib')
endif
